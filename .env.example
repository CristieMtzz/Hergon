# Railway inyecta estas variables automáticamente
# Solo necesitas este archivo para desarrollo local

# PostgreSQL (Railway lo asigna automáticamente)
# DATABASE_URL=postgresql://user:pass@host:5432/railway

# JWT Secret (Railway: agregar como variable de entorno)
JWT_SECRET=hergon-secret-key-2026

# Puerto (Railway lo asigna automáticamente)
PORT=3000
